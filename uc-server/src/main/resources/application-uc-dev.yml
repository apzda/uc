server:
  port: 30902

spring:
  application:
    name: ucenter-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/apzda_uc_db?characterEncoding=UTF-8&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: Abc12332!
    druid:
      initial-size: 5
  jpa:
    show-sql: true
  data:
    redis:
      host: localhost
      database: 1
      lettuce:
        pool:
          enabled: true

logging:
  level:
    com.apzda.cloud: trace
    web: info
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSSXXX} %highlight(%-5level) --- [%15.15t] %cyan(%-40.40logger{39}):%-4L - [%X{tid}]%msg%n"

apzda:
  cloud:
    reference:
      captchaService:
        svc-name: http://localhost:38082
      configService:
        svc-name: http://localhost:8082
      auditService:
        svc-name: http://localhost:38089
    security:
      meta-repo: redis
